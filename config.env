# ========================================
# config.env (исправленный)
# ========================================

# ВАЖНО: эту строку внутри config.env обычно НЕ нужно.
# Оставляю закомментированной, чтобы не ломать source config.env
# CONFIG_ENV=config.env

# ========================================
# 📦 ИСТОЧНИК ЯДРА (Redmi 10C / fog)
# ========================================
# MiCode/Xiaomi_Kernel_OpenSource не имеет ветки fog-s-oss, поэтому берем публичный fog-kernel
KERNEL_SOURCE=https://github.com/r0ddty/kernel_xiaomi_fog
KERNEL_SOURCE_BRANCH=andromeda-mk2
KERNEL_CONFIG=fog_defconfig
KERNEL_ZIP_NAME=KernelSU-Makeine-fog.zip
ARCH=arm64

# ========================================
# 🔧 KERNELSU
# ========================================
ENABLE_KERNELSU=true
KERNELSU_TAG=v0.9.5
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true

# ========================================
# 🧰 CLANG (ФИКС: чтобы не было refs/heads//clang-.tar.gz)
# ========================================
# Некоторые workflow игнорируют CUSTOM_CLANG и тянут AOSP clang по этим переменным.
# Поэтому задаем их явно.
CLANG_BRANCH=main
CLANG_VERSION=r416183b1
AOSP_CLANG_BRANCH=main
AOSP_CLANG_VERSION=r416183b1

# ========================================
# 🛠️ КОМПИЛЯТОРЫ
# ========================================
# Оставляем твой proton-clang (если workflow действительно использует custom clang)
USE_CUSTOM_CLANG=true
CUSTOM_CLANG_SOURCE=https://github.com/kdrag0n/proton-clang
CUSTOM_CLANG_BRANCH=master

USE_CUSTOM_GCC_64=true
CUSTOM_GCC_64_SOURCE=https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9
CUSTOM_GCC_64_BRANCH=lineage-19.1
CUSTOM_GCC_64_BIN=aarch64-linux-android-

USE_CUSTOM_GCC_32=true
CUSTOM_GCC_32_SOURCE=https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9
CUSTOM_GCC_32_BRANCH=lineage-19.1
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# ========================================
# 🚀 ТВОЙ boot.img
# ========================================
BUILD_BOOT_IMG=true
SOURCE_BOOT_IMAGE="https://tinyurl.com/fog-boot-image"

# ВАЖНО: пайп обязательно в кавычки, иначе config.env часто ломается при source
EXTRA_CMDS='curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s v0.9.5'

ENABLE_CCACHE=true
REMOVE_UNUSED_PACKAGES=true
