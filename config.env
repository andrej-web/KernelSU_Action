CONFIG_ENV=config.env
# ========================================
# КОНФИГУРАЦИЯ ДЛЯ СБОРКИ ЯДРА KERNELSU
# ========================================

# ========================================
# 📦 ИСТОЧНИК И ПАРАМЕТРЫ ЯДРА
# ========================================
# URL репозитория исходного кода ядра
KERNEL_SOURCE=https://github.com/alternoegraha/android_kernel_xiaomi_fog
# Ветка ядра для клонирования
KERNEL_SOURCE_BRANCH=15
# Имя файла конфигурации ядра
KERNEL_CONFIG=fog_defconfig
# Название выходного ZIP файла с ядром
KERNEL_ZIP_NAME=KernelSU-Makeine-fog.zip
# Архитектура процессора (arm64, arm, x86_64)
ARCH=arm64

# ========================================
# 🔧 КОНФИГУРАЦИЯ KERNELSU
# ========================================
# Включить KernelSU в ядро (true/false)
ENABLE_KERNELSU=true
# Версия/тег KernelSU для использования
KERNELSU_TAG=v0.9.5
# Добавить конфигурацию kprobes (true/false)
ADD_KPROBES_CONFIG=true
# Добавить поддержку OverlayFS (true/false)
ADD_OVERLAYFS_CONFIG=true

# ========================================
# 🛠️ КОНФИГУРАЦИЯ КОМПИЛЯТОРОВ
# ========================================
# Использовать GCC компилятор для ARM64 (true/false)
ENABLE_GCC_ARM64=true
# Использовать GCC компилятор для ARM32 (true/false)
ENABLE_GCC_ARM32=true
# Ветка Clang компилятора (обычно master-kernel-build-2022)
CLANG_BRANCH=master-kernel-build-2022
# Версия Clang компилятора (например r450784e)
CLANG_VERSION=r450784e

# ========================================
# 📝 ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ
# ========================================
# Дополнительные команды для выполнения при сборке
EXTRA_CMDS=curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s v0.9.5

# ========================================
# 🚀 ДОПОЛНИТЕЛЬНЫЕ ОПЦИИ (ОПЦИОНАЛЬНО)
# ========================================
# Название выходного файла ядра (например: Image.gz, zImage)
# KERNEL_IMAGE_NAME=Image.gz

# Добавить локальную версию в имя файла (true/false)
# ADD_LOCALVERSION_TO_FILENAME=false

# Использовать пользовательский Clang (true/false)
# USE_CUSTOM_CLANG=false

# Источник пользовательского Clang (URL или путь)
# CUSTOM_CLANG_SOURCE=https://example.com/clang.tar.gz

# Ветка пользовательского Clang
# CUSTOM_CLANG_BRANCH=master

# Пользовательские команды сборки
# CUSTOM_CMDS=

# Использовать пользовательский GCC 64-bit (true/false)
# USE_CUSTOM_GCC_64=false

# Использовать пользовательский GCC 32-bit (true/false)
# USE_CUSTOM_GCC_32=false

# Применить KSU патч (true/false)
# APPLY_KSU_PATCH=false

# Использовать пользовательский AnyKernel3 (true/false)
# USE_CUSTOM_ANYKERNEL3=false

# Включить ccache для ускорения компиляции (true/false)
# ENABLE_CCACHE=false

# Нужен ли DTBO образ (true/false)
# NEED_DTBO=false

# Собрать boot образ (true/false)
# BUILD_BOOT_IMG=false

# Отключить LTO оптимизацию (true/false)
# DISABLE-LTO=false

# Отключить CC_WERROR (true/false)
# DISABLE_CC_WERROR=false

# Удалить неиспользуемые пакеты (true/false)
# REMOVE_UNUSED_PACKAGES=false
